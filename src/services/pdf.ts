/**
 * PRO: tax summary PDF — integrate `react-native-pdf-lib` or similar when needed.
 */
export async function generateTaxSummaryPdf(_payload: Record<string, unknown>): Promise<Uint8Array> {
  void _payload;
  throw new Error('PDF generation not implemented');
}
